class Constants {
  // API - Backend Railway
  static const String apiBaseUrl =
      'https://backendihc-production.up.railway.app/api';

  // Timeouts
  static const Duration apiTimeout = Duration(seconds: 30);
}
